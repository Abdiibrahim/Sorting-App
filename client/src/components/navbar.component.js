import React, { Component } from 'react';

// Navbar component
export default class Navbar extends Component {

    render() {
        return (
            <nav className="navbar navbar-dark bg-dark navbar-expand-lg">
                <span className="navbar-brand mb-0 h1">Sorting Application</span>
            </nav>
        );
    }
}
